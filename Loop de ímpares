#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n, m;
    int x;
    scanf("%d %d", &n, &m);
    
    for(x = n; x<=m; x++){
    if(x%2 != 0)
    printf("\n%d",x);
    }   
    
	return 0;
}
