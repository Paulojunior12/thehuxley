#include <stdio.h>

int main(){
    int x,y,calc;
    scanf("%d %d", &x, &y);
    
    if(x >= 10 && y < 100) {
    
        for (; x<=y; x++){
            if( x % 10 != 0 ){
                calc = x / 10;
                
                if( x % calc == 0)
                    printf("\n%d", x);
            }
        }
    }
    return 0;
}
